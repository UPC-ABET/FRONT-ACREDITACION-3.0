# Blocks direct pushes to develop, staging and production.
# Prefix with ABET_ALLOW_PROTECTED_PUSH=1 when the direct push is deliberate.
ABET_HOOK=node_modules/abet-plugins/shared/hooks/cli.mjs
if [ -f "$ABET_HOOK" ]; then
  node "$ABET_HOOK" pre-push
fi
